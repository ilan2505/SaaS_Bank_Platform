# Used by `docker-compose up` (docker-compose auto-loads a .env file next to
# docker-compose.yml). For local (non-Docker) development, set these in
# backend/.env instead — see backend/.env.example.
ANTHROPIC_API_KEY=your-anthropic-api-key-here
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
EXTRACTION_CONFIDENCE_THRESHOLD=0.75
